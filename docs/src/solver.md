# The solver

Converting between primitive and conserved variables, using entropy and
rapidity as the iteration variables, plus the policy layer that never fails.

```@autodocs
Modules = [EntropyEOS]
Pages = ["core/prim2con.jl", "core/con2prim.jl", "core/state_policy.jl"]
```
